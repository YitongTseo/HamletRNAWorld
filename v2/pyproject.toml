[project]
name = "rna-world"
version = "0.1.0"
description = "2D RNA-like polymer folding sim with custom dynamic bonding."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "moderngl-window>=2.4",
]

[tool.setuptools]
packages = ["sim", "render"]
