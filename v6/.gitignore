# data/generations/ is the archival record and IS tracked.
# data/flasks/ and data/worms/ are running state (live poems + current
# weights) and stay ignored — see also the repo-root .gitignore.
data/flasks/
data/worms/
__pycache__/
*.pyc
.cloudflared/
